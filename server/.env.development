PORT=4000
NODE_ENV=development
JWT_SECRET=parkon-dev-secret-key-2026
JWT_EXPIRES_IN=24h
DB_PATH=./data/parkon.db
CORS_ORIGIN=http://localhost:5173

# SMTP - MailHog (dev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=parkon@company.com
